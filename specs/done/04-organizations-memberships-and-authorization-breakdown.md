# Phase 04 breakdown — commit-sized tasks

Source contract: `04-organizations-memberships-and-authorization.md` (WIP). Baseline: Phase 03 closed (`specs/done/03-*`, `docs/phases/03-identity.md`); committed tree green at **739 tests**, `ruff check`/`format` clean. Phase 04 non-goals apply throughout: no API-key endpoints (Phase 05), no invitations/email flows, no billing plans, no product-resource authorization, no DynamoDB (Phase 06), no CDK (Phase 07), no org rename/disable/role-change (no storage update methods exist — see decision 3).

## Precondition (planner/agent action, before task 1)

The tree is dirty from the Phase 03 close-out plus an **abandoned scaffold** from a prior session. One `chore(specs)` commit stages **exactly** the specs moves: deletions `specs/wip/03-*.md` (2) and `specs/draft/04-*.md`; additions `specs/done/03-identity-resolution-and-provisioning.md`, `specs/done/03-identity-resolution-and-provisioning-breakdown.md`, `specs/wip/04-organizations-memberships-and-authorization.md`, this breakdown, and the reviewer-preserved original `specs/draft/04-organizations-memberships-and-authorization-breakdown.original.md`. No `src/`, no `NOTES.md`, no `.pi/` in this commit. Stage explicitly by path (`git add -A -- specs/` or the listed files) and verify `git status` shows exactly these 8 paths before committing — **nothing is currently staged** (`git diff --cached` is empty), so a bare `git commit` (no `-a`) would commit nothing.

**Scaffold disposition (decision 0):** the untracked stubs `src/app/api/{organizations,members}.py` and `src/app/services/{organization,member}.py` are a `NotImplementedError` skeleton from a prior run and are **replaced wholesale** by tasks 2–5 (their docstrings advertise `PUT/DELETE /v1/organizations/{id}` routes that do **not** exist in the frozen manifest — nothing from them survives; `member.py` does not even import `OrganizationId`/`UserId` it references, and the stubs are the source of the 83 working-tree ruff errors). `NOTES.md` and `.pi/` stay untracked and are never committed; task 7 deletes `NOTES.md`.

## Planner decisions (interpretation of the contract)

1. **Frozen surface is untouched.** The manifest (`endpoint_for`), all request/response schemas, the `Error` envelope, the `Storage` protocol's existing 18 methods, and `build_current_user`/`resolve_or_provision`/`build_user_context` are consumed as-published by Phase 03; no signature changes. Routers register **from the manifest entries themselves** (the `me.py` pattern) and take `build_organizations_router(storage, verifier)` / `build_members_router(storage, verifier)`, mirroring `build_me_router`.

2. **New storage compound `provision_organization` (explicit contract update, not smuggled).** Organization creation must atomically write org + **owner** membership + two audits, because the owner invariant (decision 3) has no repair path: sequential `create_organization` → `create_membership` under per-op transactions can crash between the writes, leaving a permanent ownerless org whose slug is consumed forever (retry → 409, no update/delete-organization exists). AGENTS.md ("provisioning … concurrency-sensitive: define and test atomicity") makes this the sanctioned move under the delivery-map review rule: an explicit addition to the **current** phase's contract. Shape mirrors `provision_user` exactly: frozen echo bundle `ProvisionedUser`-style `ProvisionedOrganization(organization, membership, audit_events)`; keyword-only `(organization=…, membership=…, audit_events=…)`; one `BEGIN IMMEDIATE` transaction over the existing `_insert_*_row` helpers; UNIQUE violations translate to `DuplicateEntityError(kind=organization_slug / entity_id)`; unknown `membership.user_id` → `ReferenceNotFoundError` (org and audit FKs are satisfied inside the batch); full rollback on every failure; storage mints nothing. **No race-convergence semantics** (unlike `provision_user`): a slug conflict is a plain 409, never a converge. Task 1 must also update the contract module docstring ("18 operations" → 19) and **replace** the `get_user_by_external_identity` sentence "there is no 19th method on this protocol" — after this addition the literal is false, so it becomes "there is no *separate* `resolve_external_identity` method on this protocol" (keeping the `resolve_external_identity` substring that `test_storage_contract.py:432` pins), and the paired assertion at line 433 (`assert "no 19th method" in doc`) is updated **in the same commit** to pin the new phrase — never kept alive by preserving the stale literal. The stale adapter comment `sqlite.py:1258` ("the contract gains no 19th method") gets the same reword. Also update the pinned-surface test (18→19 + name list), `docs/contracts.md` (docs change rule: same change as the contract), and add suite cases **before** any service relies on it (AGENTS.md). DynamoDB replication is a Phase 06 obligation carried by the contract docstring, same discipline as `provision_user`.

3. **Documented role policy (the phase spec's "explicit role policy"; spec-revision proposal, §4/§14).** Role rank is `viewer < member < admin < owner` (`ROLE_RANK: dict[MembershipRole, int]`). Reads (`GET /v1/organizations`, `GET /v1/organizations/{id}`, `GET .../members`) require an **active membership at rank ≥ viewer** (any role). Mutations (`POST .../members`, `DELETE .../members/{user_id}`) require rank ≥ **admin** (owner or admin). `POST /v1/organizations` requires only authentication; the creator becomes **owner** via the batch. **Owner invariants:** the `owner` role is (a) not grantable through the membership API, (b) not removable through it (nobody, including owners, can delete an owner membership), and (c) not changeable — enforced by policy plus the deliberate absence of any update method in storage. Consequences documented: leaving an organization you own is impossible; **member/viewer cannot self-remove either (removal requires rank ≥ admin), while an admin may remove themselves or a peer admin** (no admin-vs-admin special case is invented); every org keeps exactly its creator/provisioner as owner; adding to a `disabled` membership's pair is a 409 (pair uniqueness covers it) and there is no re-enable path — `MembershipStatus.DISABLED` is never set by Phase 04 (no update op).

4. **Denial semantics: one 403, no existence oracle, auditable where the FK allows.** Unknown organization, inactive organization, missing membership, inactive membership, and insufficient role all answer **403 `forbidden` with one fixed message** ("you do not have permission to access this organization") — deliberately mirroring Phase 03 decision 9's pinned explicit-org branch (403, no oracle), so the two seams never disagree. `authorization.denied` is appended **whenever the organization row exists** (audit→organization FK is then satisfiable): metadata is exactly `{"reason": <no_membership|inactive_membership|inactive_organization|insufficient_role>, "operation": <manifest operation_id>}` — reason strings are the only vocabulary; no email, no `sub`, no token, no role of the caller beyond what the reason implies. **`classify_access` checks in fixed precedence — organization status, then membership presence, then membership status, then role rank — so when several conditions hold at once (e.g. disabled org *and* absent membership) the audit `reason` is deterministic** (the uniform 403 keeps oracle-freedom; the precedence exists for audit forensics and must be pinned by the task-2 matrix). Unknown-organization denials get the same 403 but **cannot be audited** (no FK target) — documented exception to AC 4; the cross-tenant acceptance cases use existing orgs. Audit-append failure propagates (fail-closed: a denial that cannot be audited is a 500, never a silent 403). Authentication failures (401) precede the dependency and are not authorization denials — no audit. A request that will be denied still auto-provisions a first-seen Cognito identity (authn precedes authz, spec §6) — consistent with `/v1/me`, documented.

5. **Path parameter is the tenant selector.** Org-scoped routes act on `{organization_id}` from the path; the `AuthorizationContext.organization_id` (earliest-active default from `build_current_user`) is **not** required to match — otherwise a user could only manage their earliest org. The reusable contract is keyed on the resolved context so Phase 05 can add the `api_key` branch: the dependency consumes `ResolvedIdentity` and checks membership of `context.actor_id` (a `usr_` this phase). Org-selection headers/params are not invented (no spec basis).

6. **Layering per AGENTS.md.** Pure authorization rules + audit-event builders + domain errors live in `src/app/services/authorization.py` (tenancy/audit business rules); the FastAPI wiring (compose `build_current_user` → classify access → denial audit → `HTTPException`) lives in `src/app/auth/organization_access.py` (authorization resolution); organization/member mutation rules live in the rewritten `src/app/services/{organization,member}.py` as **functions with injected `storage` and injectable `now`/`ids`** (the Phase 03 `identity.py` pattern — the class-based scaffold is dropped); HTTP status translation for service errors happens in the routers (`app/api`). Mapping table (frozen envelope codes, no new codes): `OrganizationTypeNotSelectableError`/`OwnerRoleNotAssignableError` → **400** `validation_error`; `MemberNotFoundError` (remove a non-member) / `TargetUserNotFoundError` (add an unknown `usr_`, translated from `ReferenceNotFoundError` — org FK is already proven by the dependency) → **404** `not_found`; `OrganizationSlugConflictError` (from `kind=organization_slug`), `MembershipConflictError` (from `kind=membership`), `OwnerMembershipImmutableError` → **409** `conflict`; `InvalidCursorError` from list endpoints → **400** `validation_error` (opaque client-supplied token is malformed/foreign); any other `StorageError` → **untranslated** → the frozen 500 `internal_error` handler (adapter text never echoed). Error messages are fixed strings; no identifiers in error bodies (IDs live in audits, not responses).

7. **Mutation audits (JSON-safe, secret-free, targets pinned).** `organization.created`: metadata `{"type": <org type>}`, target `organization`/new `org_`, actor the creator `usr_` — inside the `provision_organization` batch. `membership.created` (API add): metadata `{"role": <granted role>}`, target `membership`/new `mem_`, actor the admin `usr_`, organization the org — standalone `append_audit_event` **after** the successful write. `membership.removed`: metadata `{"role": <role at removal>}`, target `membership`/the removed `mem_` id (target_id is a plain string, no FK) — appended **after** the delete commits. Ordering rationale: a mutation that fails must not audit as success; a committed mutation whose audit append fails returns 500 with the mutation persisted — accepted, documented limitation (SQLite has no cross-call transaction; the compound batch is the only atomic unit).

8. **List projection.** Services return domain `Page[Organization]` / `Page[Membership]`; routers project to `Page[OrganizationResponse]` / `Page[MemberResponse]` field-by-field, preserving `limit` and `next_cursor` verbatim (cursors stay opaque). `MemberResponse` deliberately omits the `mem_` record id (frozen schema). Query binding: `Annotated[PageParams, Query()]` (FastAPI 0.141 supports Pydantic-model query params; pinned version range `>=0.115,<1`). `list_memberships` returns **all statuses** by contract — MemberResponse carries `status`, so disabled rows are visible in the list (documented, matches contract, no filter invented).

9. **Test strategy (mirrors Phase 03's proven stack).** Unit: stub `Storage` classes recording calls (zero-write proofs) for `services/{authorization,organization,member}.py`; SQLite-backed service tests on tmp files. Integration: `TestClient(create_app(routers=[...]))` with the signed-token fixtures + loopback `JwksTestServer` from `src/tests/support/cognito.py` (never live Cognito), real SQLite, and a `seed_user(storage, sub, …)`-style helper that creates user + identity (+ org/membership with a chosen role) directly — role-matrix users are seeded, not provisioned. **Audit assertions read the SQLite file directly** (no audit read surface in the contract; precedent: Phase 03 task 6's direct-row counts). Concurrency: `threading.Barrier`, no sleeps, 20 repeats, main-thread warm-up read before the barrier. Every cross-tenant case asserts both the 403 body equality (oracle-freedom) and the audit row.

## Tasks

1. **`provision_organization` storage compound**
   - Files: `src/app/storage/contract.py`, `src/app/storage/sqlite.py`, `src/tests/storage_contract/suite.py`, `src/tests/unit/test_storage_contract.py`, `src/tests/unit/test_sqlite_provision_org.py`, `docs/contracts.md` (protocol bullet 18→19 + the new operation).
   - Content: `ProvisionedOrganization` frozen echo bundle; `Storage.provision_organization(*, organization, membership, audit_events) -> ProvisionedOrganization` with the docstring pinned per decision 2 (incl. the explicit Phase 06 replication obligation); `SQLiteStorage` implementation reusing `_insert_*_row` helpers in one `BEGIN IMMEDIATE` transaction; contract-docstring count reword + the `get_user_by_external_identity` "no 19th method" sentence **and** its pinned test assertion + the `sqlite.py:1258` comment all replaced per decision 2 (single consistent truth: the protocol now has 19 methods; there is no separate resolve method).
   - Verify (contract tests land in the same commit, before any consumer): suite — atomic write with full read-back of all three row kinds; slug conflict → `DuplicateEntityError(kind=organization_slug)`; taken `org_`/`mem_`/`aud_` ids → `kind=entity_id`; unknown `membership.user_id` → `ReferenceNotFoundError`; rollback proof — a rejected batch leaves zero org/membership/audit rows. `uv run pytest` green (739 + new), ruff check/format clean.

2. **Authorization rules + audit helpers**
   - Files: `src/app/services/authorization.py`, `src/tests/unit/test_authorization_rules.py`.
   - Content: `ROLE_RANK`; `AccessDecision` outcome vocabulary (`granted`/`no_membership`/`inactive_membership`/`inactive_organization`/`insufficient_role`) and a **pure** `classify_access(organization, membership|None, min_role) -> AccessDecision` (None membership = absent); domain errors + fixed messages per decision 6; `build_organization_created_audit`, `build_membership_created_audit`, `build_membership_removed_audit`, `build_denial_audit(...)` (exact metadata per decisions 4/7, ids/clock injected); `audit_denial(storage, actor_user_id, organization_id, reason, operation, *, now)` appending via `append_audit_event`; `require_selectable_organization_type(type)` (customer only) and `require_assignable_member_role(role)` (not owner) guards.
   - Verify: stub-storage unit tests — denial audit shape exactly `{"reason", "operation"}` with actor `usr_`/org FK fields and no email/sub/token substrings in any field; classification matrix over (absent/disabled/active membership) × (active/disabled org) × (four roles) × (viewer/admin minimums), **including the decision-4 precedence cells (e.g. disabled org + absent membership ⇒ `inactive_organization`)**; audit builders deterministic under injected `now`/ids; guards raise the pinned errors. No FastAPI imports in this module.

3. **Shared organization-access dependency**
   - Files: `src/app/auth/organization_access.py`, `src/app/auth/__init__.py` (re-exports), `src/tests/unit/test_organization_access.py`.
   - Content: `OrganizationAccess` (frozen: `identity`, `organization`, `membership`); `build_organization_member_dependency(storage, verifier, operation_id)` and `..._admin_dependency(...)` composing `build_current_user` → `get_organization` + `get_membership` (EntityNotFound → decision-4 uniform 403) → `classify_access` → `audit_denial` when org exists → 403, else grant. Fixed single 403 message; `HTTPException` only (routers add no authz logic).
    - Verify: unit tests over a probe router defined **in the test module** (the design rule is that *shipped* routers register manifest entries only; nothing scans test modules) with the Phase 03 fake-verifier/SQLite pattern — each of the five decision-4 outcomes returns the byte-identical 403 envelope; audit row present for the four org-exists cases and provably absent for unknown-org; insufficient-role appends `insufficient_role` **before** the handler body runs (probe handler records it was never called); grant path exposes org+membership to the handler.

4. **Organization endpoints**
   - Files: `src/app/services/organization.py` (rewrite per decision 0), `src/app/api/organizations.py` (rewrite), `src/tests/integration/test_organizations_endpoints.py`.
   - Content: service `create_organization(storage, creator_user_id, name, slug, type, *, now=None, ids=None)` (type guard → batch build → `provision_organization` → slug-conflict translation) and `list_organizations(storage, user_id, page)` pass-through; router registers `list_organizations`/`create_organization`/`get_organization` from `endpoint_for` specs; GET uses the member dependency (200 = `OrganizationResponse` projection); list binds `PageParams` per decision 8; `InvalidCursorError` → 400.
   - Verify: integration tests — create 201 returns the org and (direct SQLite read) exactly org + owner membership + `organization.created`/`membership.created` audits sharing one timestamp; `type=personal|internal` → 400 with **zero rows written**; taken slug (seeded org, incl. a `personal-*` collision) → 409; list shows only the caller's active-membership orgs and never the other's (two seeded users, two orgs); limit clamps (`limit=1000` → page `limit=100`), cursor round-trips, foreign cursor → 400; **AC-5 role matrix on all three org routes** (seeded owner/admin/member/viewer in one org): GET /{id} 200 for **each of the four roles** + outsider 403+audit with the byte-identical body, list 200 for each role showing only own orgs, create 201 for each role (authenticated-only per decision 3); GET unknown org → the identical 403 body, no audit row; every error matches the frozen envelope.

5. **Member endpoints**
   - Files: `src/app/services/member.py` (rewrite per decision 0), `src/app/api/members.py` (rewrite), `src/tests/integration/test_members_endpoints.py`.
   - Content: service `add_member(storage, actor_user_id, organization_id, user_id, role, *, now=None, ids=None)` (owner-role guard → `create_membership` → `ReferenceNotFoundError`→`TargetUserNotFoundError`, `kind=membership`→`MembershipConflictError` → `membership.created` audit after success) and `remove_member(storage, actor_user_id, organization_id, user_id, *, now=None)` (`get_membership` first: miss → `MemberNotFoundError`; owner → `OwnerMembershipImmutableError`; `delete_membership`; `membership.removed` audit after commit); `list_members` pass-through. Router: list + member dependency, create/remove + admin dependency; 204 delete with empty body (manifest-pinned); decision-6 mapping.
   - Verify: integration tests over a seeded **owner/admin/member/viewer matrix** in one org + an outsider: list — any member role 200 (all statuses visible, org-scoped, outsider 403+audit); add — owner & admin 201 with audit, member & viewer 403+`insufficient_role` audit, `role=owner` → 400, existing pair (and disabled-pair) → 409, unknown target `usr_` → 404; remove — admin removing member/viewer 204 + `membership.removed` (role-at-removal in metadata), removing **owner** → 409 whether by admin or the owner self, non-member → 404, member/viewer callers → 403+audit; cross-tenant: outsider attempts on all three member routes → 403+audit, **zero mutation** (recording counts via direct reads); response bodies contain no `mem_` ids and no emails.

6. **Concurrency + duplicate-request + audit-hygiene acceptance proofs**
   - Files: `src/tests/integration/test_org_membership_concurrency.py`, `src/tests/integration/test_audit_hygiene.py`.
   - Content: barrier cases (20 repeats each, per decision 9): 8 threads `POST /v1/organizations` same slug → exactly one org + one owner membership + two audits, losers 409; 8 threads add the same target user → one membership, losers 409; concurrent remove of one pair → exactly one 204 and one 404 (contract pins delete non-idempotent — behavior proven, documented). Hygiene sweep: run the full mutation+denial battery, then read **every** audit row directly and assert action ∈ the §16 vocabulary, metadata keys ⊆ the decision-4/7 pinned sets, and no row contains `@`, `sub`-like, or token-like material.
   - Verify: no sleeps; stable across the 20 repeats; final counts asserted via direct SQLite reads (Phase 03 task 6 pattern).

7. **Handoff documentation and green sweep**
   - Files: `docs/phases/04-organizations.md` (new), `docs/README.md`, `docs/architecture.md`, `docs/contracts.md`, `README.md`, this breakdown (evidence + DoD check); delete `NOTES.md`.
   - Content: the reusable contracts Phases 05/06/08 code against — `require_organization_member/admin` dependency factories + `classify_access`/`ROLE_RANK` policy, the decision-4 denial semantics, `provision_organization` (Phase 06 must replicate atomically), the extended audit vocabulary (`membership.removed`, `authorization.denied` + metadata shapes), and the decision-3 owner invariants with their consequences (no owner transfer/leave); known limitations: no rename/disable/role-change (no storage update ops), disabled memberships are dead state, member/viewer cannot self-remove (decision 3), add-member distinguishes unknown-target 404 from duplicate-pair 409 — an org admin can therefore probe whether a `usr_` exists globally, accepted because IDs are uuid4-based and unguessable, audit-append-after-commit window, un-audited unknown-org 403, `internal` orgs unmanageable via API. Follow `docs/requirements.md`: no links from `docs/` to WIP/draft specs.
   - Verify: `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `git diff --check` green; record command-output summary as evidence; the no-`boto3` import proof stays green (Phase 04 adds no runtime deps).

## Definition of done check (maps to acceptance criteria)

- [x] A user lists only memberships and cannot read another organization by guessed ID (tasks 4, 5 — list scoping + uniform-403 GET matrix)
- [x] Only documented roles mutate organizations/memberships; out-of-policy attempts are denied consistently (tasks 2, 3, 4, 5 — decision-3 policy + byte-identical 403)
- [x] Membership creation/removal is duplicate-safe and preserves documented owner invariants (tasks 1, 2, 5, 6 — 409 paths, owner immutability, barrier proofs)
- [x] Authorized mutations create safe audit events; denials create `authorization.denied` without sensitive data (tasks 2, 4, 5, 6 — direct-row assertions + hygiene sweep; unknown-org no-FK exception documented per decision 4)
- [x] Integration tests cover owner/admin/member/viewer decisions and cross-tenant denial for every endpoint (tasks 4, 5 — all six §14 endpoints of this phase)

## Delivery evidence (task 7)

Commits: precondition `chore(specs)` (specs moves only), then
`feat(phase-04 task 1..7)` per this breakdown. The abandoned scaffold
(decision 0) was deleted uncommitted and replaced wholesale by tasks 4/5;
`NOTES.md` deleted; `.pi/` never committed.

Final sweep (2026-09-13):

- `uv run pytest` → **943 passed** (baseline 739 + 204 Phase 04: task 1 19,
  task 2 69, task 3 7, task 4 23, task 5 24, task 6 62).
- `uv run pytest src/tests/storage_contract` → 64 passed (60 adapter-neutral
  suite cases — including the four new `provision_organization` proofs — +
  4 harness isolation cases); DynamoDB entry deferred to Phase 06 per plan.
- `uv run ruff check .` → All checks passed; `uv run ruff format --check .`
  → clean; `git diff --check` → clean.
- No-`boto3` subprocess import proof (`test_app_skeleton.py`) green;
  Phase 04 added no runtime dependencies (`pyproject.toml` untouched).
- Handoff: `docs/phases/04-organizations.md` (new) linked from
  `docs/README.md`; `docs/architecture.md`, `docs/contracts.md`, and
  `README.md` updated in the same commit; no docs link to WIP/draft specs.

Escalations below remain open as spec-revision proposals (planner record,
not implementation work); Phase 06/07 obligations and handoff notes are
recorded in the phase doc's "Published interfaces" and this file.

## Escalations (planner action, not implementation work)

- **Spec-revision proposals** (delivery-map review rule): (1) §12 — add the `provision_organization` compound (this breakdown's decision 2 makes it an explicit Phase 04 contract addition); (2) §4/§14 — record the decision-3 role policy and owner invariants (the spec names roles but never assigns powers); (3) §14 — name the add-member outcomes the spec leaves open: unknown target 404, duplicate pair 409; (4) §16 — pin `authorization.denied` metadata shape `{reason, operation}` and record that a denial for a nonexistent organization is structurally unauditable (the §4 `AuditEvent.organization_id` is required and FK-bound), so "denials create authorization.denied" admits exactly that exception.
- **Phase 06 obligation:** replicate `provision_organization` atomics + run the extended conformance suite against DynamoDB Local (contract docstring carries the duty, same as `provision_user`).
- **Phase 07 handoff note:** deployment entrypoint mounts `build_me_router` + `build_organizations_router` + `build_members_router` on the same storage/verifier instances.
- **Deferred, explicit (not smuggled):** org rename/disable and membership role changes/re-enable (need storage update ops — no spec basis this phase); `internal`-org operator management; org-selection header; audit read surface (Phase 08); invitations (phase non-goal).

## Review log

- **@reviewer 2026-09-13: APPROVE-WITH-CHANGES (applied).** All on-disk claims re-verified true (739 green tests, 83 ruff errors confined to the 4 untracked stubs, 18-method contract + "no 19th method" pin at `test_storage_contract.py:433`, manifest statuses, FK/UNIQUE schema facts, `personal-{usr_}` default slug, FastAPI 0.141.1). Changes applied to this file (original preserved at `specs/draft/04-...-breakdown.original.md`, to be swept into the precondition commit): B1 — decision 2/task 1 now *replace* the "no 19th method" literal (docstring, `sqlite.py:1258` comment, and the pinned assertion) instead of trying to keep a false sentence passing; B2 — task 4 verify now pins the owner/admin/member/viewer × outsider matrix on **all three org routes** (AC 5 says "every endpoint"; task 5 already did for member routes). Non-blocking: denial-reason precedence pinned (decision 4/task 2), self-removal + user-existence-probe limitations documented (decisions 3/6, task 7), §16 escalation extended with the unauditable-unknown-org exception, precondition commit given explicit staging instructions. Verdicts on the six scrutiny areas: (1) 19th-method addition — **approve** (spec §12 expressly permits atomic compound domain ops; ownerless-org crash window is unrecoverable without it; not smuggled — explicit contract addition + escalation + Phase 06 obligation, same discipline as `provision_user`; no-race-convergence is correct); (2) uniform 403 — **approve** (mirrors pinned Phase 03 decision 9; the unknown-org audit exception is structurally forced by the §4 FK and now escalated to §16 rather than papered over; fail-closed 500 on audit-append failure is the right reading of "denials create authorization.denied"); (3) role policy — **approve** (viewer<member<admin<owner rank, owner immutability enforced by policy *and* absent update ops; self-removal consequences now explicit); (4) status mapping + audit-after-commit — **approve** (uses only frozen codes; untranslated StorageError→500 matches `errors.py`; the documented audit window is the honest SQLite-boundary tradeoff); (5) granularity — **approve** (7 commits, contract-test-before-consumer honored, scaffold disposition correct); (6) AC coverage — **approve after B2** (all five ACs now map to pinned tests).
