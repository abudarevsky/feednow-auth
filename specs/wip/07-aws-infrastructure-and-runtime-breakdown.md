1. Scaffold the Python CDK entrypoint with environment-parameterized inputs
   - Scope: deploy/aws/cdk/app.py, deploy/aws/cdk/cdk.json, deploy/aws/cdk/requirements.txt, deploy/aws/cdk/.env.example, deploy/aws/cdk/feednow_auth_stack.py (minimal stack class), pyproject.toml (dev group), src/tests/unit/test_cdk_app.py
   - Change: Follow Vispector's entrypoint pattern (../vispector/deploy/aws/cdk): app.py loads a non-committed `deploy/aws/cdk/.env` without overriding the shell, requires non-secret inputs `FEEDNOW_ENV` (dev|staging|prod), `CDK_DEFAULT_ACCOUNT`, and `AWS_REGION`, then synthesizes `FeedNowAuthStack` bound to `cdk.Environment(account, region)`; cdk.json is `{"app": "python3 app.py"}`; requirements.txt pins `aws-cdk-lib` and `constructs`; add both to the pyproject dev dependency group so assertion tests run under `uv run pytest`; the stack module exposes a `FeedNowAuthEnv` value object that validates `FEEDNOW_ENV` and derives all names from it (stack `FeedNowAuth-<env>`, resource/table/Cognito-domain prefix `feednow-auth-<env>-`/`feednow-auth-<env>`); .env.example contains placeholders only — no account ids, no secret material.
   - Verify: `FEEDNOW_ENV=dev CDK_DEFAULT_ACCOUNT=123456789012 AWS_REGION=eu-north-1 npx -y aws-cdk@2 synth --quiet` run from `deploy/aws/cdk` succeeds with those placeholder values (record the exact command as evidence); `PYTHONPATH=. uv run pytest src/tests/unit/test_cdk_app.py` covers .env loading precedence, env-name validation rejection, and derived naming.
   - Depends on: none

2. Declare the seven DynamoDB tables from the Phase 06 SCHEMA
   - Scope: deploy/aws/cdk/feednow_auth_stack.py, src/tests/unit/test_cdk_dynamodb.py
   - Change: add `Table` constructs transcribed verbatim from `SCHEMA` in src/app/storage/dynamodb.py (docs/phases/06-dynamodb.md table): `users`, `organizations`, `external_identities`, `audit_events`, `api_keys` (+GSI `by-organization`), `memberships` (+GSIs `by-organization`, `by-user`), `unique_constraints`; names are `<feednow-auth-<env>-> + table name` matching the runtime `table_prefix`; GSIs use ALL projection; billing is `PAY_PER_REQUEST` in all environments; `RemovalPolicy.DESTROY` for dev/staging and `RETAIN` for prod; point-in-time recovery enabled for prod only; the stack exposes the resolved `table_prefix` string for the runtime wiring (task 6).
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_cdk_dynamodb.py` — `aws_cdk.assertions.Template.from_stack` finds exactly 7 `AWS::DynamoDB::Table` with the expected partition/sort keys and GSI names/key schemas, prefix differs per env parameter, and no `EncryptionKey`/`BillingMode` drift.
   - Depends on: 1

3. Add the Cognito user pool, public PKCE app client, and hosted domain
   - Scope: deploy/aws/cdk/feednow_auth_stack.py, src/tests/unit/test_cdk_cognito.py
   - Change: add `UserPool` with email as the only sign-in attribute (`username_attributes=[email]`), `self_sign_up_enabled=True`, email-verified password reset via Cognito default email style; add user pool client `feednow-auth-<env>` with `generate_secret=False`, OAuth authorization-code grant, scopes `openid/email/profile`, callback/logout URLs taken from the **required** non-secret `.env` input `COGNITO_CALLBACK_URLS` (comma-separated, placeholder in .env.example; the stack raises at synth when it is empty because CDK validates OAuth redirect URIs at synth time — this is why the task-6 synth command must supply it), and explicit auth flows `ALLOW_USER_PASSWORD_AUTH` + `ALLOW_REFRESH_TOKEN_AUTH` (required by the task-7 non-prod smoke path; PKCE-capable public client); add `UserPoolDomain` with prefix `feednow-auth-<env>`; export pool id, issuer URL (`https://cognito-idp.<region>.amazonaws.com/<pool>`, derived via `Fn::Sub` where account/pool are unresolved), and client id as stack outputs.
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_cdk_cognito.py` — assertions on `UsernameAttributes=["email"]`, `GenerateSecret=false`, `AuthorizationCodeGrant=true`, `ExplicitAuthFlows` containing `ALLOW_USER_PASSWORD_AUTH`, domain resource present, and outputs non-empty.
   - Depends on: 1

4. Create the generated pepper secret and the least-privilege Lambda role
   - Scope: deploy/aws/cdk/feednow_auth_stack.py, src/tests/unit/test_cdk_iam.py
   - Change: add `Secret` named `feednow-auth/<env>/api-pepper` with `Secret.generate_secret_string(generate_string_key="pepper", byte_length=48, exclude_punctuation=True)` so no plaintext pepper ever appears in source, `.env`, outputs, or the template; add the Lambda execution `Role` with exactly the per-resource grants listed in the consolidated matrix in docs/phases/06-dynamodb.md: (a) per-table `dynamodb` action grants matching exactly the actions listed per resource in that matrix (e.g., `external_identities`/`audit_events` are `PutItem` only; `BatchGetItem` only where the matrix lists it), GSI ARNs get `Query` only, with `PutItem`/`ConditionCheckItem` statements pinned by `"StringEquals": {"dynamodb:EnclosingOperation": "TransactWriteItems"}`; (b) `secretsmanager:GetSecretValue` on the pepper secret ARN only; (c) `logs:CreateLogStream`/`logs:PutLogEvents` on the function's log group ARN pattern only; no wildcard actions or resources, no `dynamodb:Scan`, no `CreateTable`/`DeleteTable` on the runtime role.
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_cdk_iam.py` — positive checks that each table/index/action pair from the matrix is present with the `EnclosingOperation` condition, and negative checks: no `"Action": "*"`, no `dynamodb:Scan`, no non-`GetSecretValue` secretsmanager action, no table/index statement grants any action outside its matrix row (per-resource action-set equality), and template JSON contains no literal secret value.
   - Depends on: 2

5. Build the Lambda runtime packaging and the composition-root handler
   - Scope: deploy/aws/lambda-requirements.txt, deploy/aws/runtime/handler.py, deploy/aws/runtime/secrets_pepper.py, pyproject.toml (dev group), src/tests/unit/test_runtime_handler.py
   - Change: lambda-requirements.txt pins the Lambda payload deps (`fastapi`, `pydantic`, `pyjwt[crypto]`, `boto3`, `mangum`); add `mangum` to the pyproject dev dependency group so the handler module is importable under `uv run pytest`; `secrets_pepper.py` implements `SecretsManagerPepper` (satisfies the `PepperSource` protocol from src/app/auth/pepper.py, which mandates the AWS source live at the deployment entrypoint): reads secret id from env, fetches `GetSecretValue` once at cold start, parses the `pepper` JSON field, enforces the ≥32-byte floor, and never renders the value in `repr`/`str`/errors; `handler.py` is the composition root: at cold start (never at import) reads `FEEDNOW_DYNAMODB_REGION`, `FEEDNOW_TABLE_PREFIX`, `FEEDNOW_COGNITO_ISSUERS`, `FEEDNOW_COGNITO_CLIENT_IDS`, `FEEDNOW_PEPPER_SECRET_ID`, then wires `CognitoJwksSource` → `CognitoAccessTokenVerifier` → `open_dynamodb_storage(region=..., table_prefix=...)` → `build_me_router`/`build_organizations_router`/`build_members_router`/`build_api_keys_router(storage, verifier, pepper)` → `create_app(routers=[...])` and exposes `handler = Mangum(app, api_stage="$default")`; `src/app` is not modified.
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_runtime_handler.py` (importlib-load the deploy modules; assert importing `handler` performs no env reads or AWS I/O; assert `build_app()` with injected fakes mounts exactly the manifest routes; assert `SecretsManagerPepper` redaction and floor enforcement); `PYTHONPATH=. uv run pytest src/tests/integration/test_dynamodb_isolation.py src/tests/integration/test_app_skeleton.py` still pass (no boto3 in `src/app`).
   - Depends on: none
   - Implemented-note (task 5): the literal `Mangum(app, api_stage="$default")` is not constructible — no released Mangum (0.5—0.22) accepts an `api_stage` keyword; HTTP API v2 events carry the stage inside the payload and Mangum infers the handler from it. The entrypoint therefore exposes `API_STAGE = "$default"` as the documented stage constant (consumed by task 6's `HttpApi`) and builds `Mangum(app)` over a lazy ASGI proxy so the composition happens at cold start, never at import. Task 6 must mount the `HttpApi` at the `$default` stage; do not reintroduce the keyword.

6. Wire the Lambda function, HTTP API Gateway, and redaction-safe access logs
   - Scope: deploy/aws/cdk/feednow_auth_stack.py, src/tests/unit/test_cdk_lambda_api.py
   - Change: add `Function` (runtime `python3.13`, x86_64, 512 MB, 30 s timeout, handler `handler.handler`) whose code is asset-bundled from `deploy/aws/runtime/*.py` + `src/app` (copied as `app/`) + lambda-requirements.txt via a Docker-free local bundling class modeled on Vispector's `_LocalBundling` (uv installs manylinux wheels); set the five `FEEDNOW_*` env vars from stack-derived values (region, task-2 table prefix, task-3 issuer/client outputs, task-4 secret name); attach the task-4 role; add `HttpApi` with `$default` stage, `ANY /` and `ANY /{proxy+}` Lambda-proxy integrations, and a custom access-log format containing only request id, http method, path, status, and integration latency — no `$context.requestHeader.*`, no `$context.requestQueryString`; grant API-to-Lambda invoke permission scoped to the function ARN.
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_cdk_lambda_api.py` — function env vars reference the pool/secret/table resources, role is the task-4 role, routes and permissions present, access-log format string contains no `requestHeader`/`requestQueryString` token; `FEEDNOW_ENV=dev CDK_DEFAULT_ACCOUNT=123456789012 AWS_REGION=eu-north-1 COGNITO_CALLBACK_URLS=https://example.invalid/callback npx -y aws-cdk@2 synth --quiet` run from `deploy/aws/cdk` succeeds without Docker.
   - Depends on: 1, 2, 3, 4, 5

7. Add the non-production deployed smoke script
   - Scope: deploy/aws/smoke/smoke.py, src/tests/unit/test_smoke_script.py
   - Change: a runnable script (operator AWS credentials, dev/staging only — refuses `prod` unless an explicit `--force` flag is passed) with injectable seams (`cognito_client`, `http_get`, `dynamodb_resource`) implementing the §5/§6 proof path: `sign_up` with a random `smoke+<ts>@example.com` and chosen password → `admin_confirm_sign_up` → `initiate_auth(USER_PASSWORD_AUTH)` for the access token → `GET https://<api>/v1/me` with the bearer token, asserting 200 and that the `MeResponse` body's user id starts with `usr_` (the payload contains only user fields — do not assert organization data in the HTTP response) → DynamoDB `GetItem` on `<prefix>unique_constraints` for `external_identity#cognito#<sub>#`, and on `<prefix>users` for the mapped user, then `Query` on the `<prefix>memberships` `by-user` GSI keyed by the `usr_` id to resolve `organization_id` (no earlier step yields it), then `GetItem` the membership pair — proving first-login provisioning and default tenancy persisted server-side; prints only ids and a pass line — never tokens, passwords, or key material; procedure steps (prereq: deployed dev stack from task 6) documented in the script docstring.
   - Verify: `PYTHONPATH=. uv run pytest src/tests/unit/test_smoke_script.py` — fake seams assert the exact call sequence, prod refusal, and (via capsys) that no token/password literal appears in output; `uv run ruff check deploy/aws/smoke` clean.
   - Depends on: 6

8. Record full verification and the Phase 07 documentation handoff
   - Scope: docs/phases/07-aws-infrastructure.md (new), docs/README.md, docs/operations.md
   - Change: write the phase 07 handoff doc per the phase Handoff section: CDK synth/deploy commands per environment, deployment input names (names only, no values), runtime configuration keys and their injection path, the smoke procedure with the recorded dev-run evidence, and rollback-safe operational notes (stack delete semantics vs. prod `RETAIN` tables, pepper secret rotation pointer to Phase 08); append a Phase 07 entry to the docs/README.md reading order and a "Deployed runtime" section to docs/operations.md; do not link docs to WIP specs.
   - Verify: `PYTHONPATH=. uv run pytest -q`, `PYTHONPATH=. uv run ruff check .`, `PYTHONPATH=. uv run ruff format --check .`, `git diff --check` all clean; every command written in docs/phases/07 is copy-pasteable from the repo root or the cdk directory as marked.
   - Depends on: 1, 2, 3, 4, 5, 6, 7
