# Phase 05 breakdown — commit-sized tasks

Source contract: `05-api-key-credentials-and-scopes.md` (WIP). Baseline: Phase 04 closed (`specs/done/04-*`, `docs/phases/04-organizations.md`); working tree verified green at **943 tests** (re-run 2026-09-13), `ruff check`/`format` clean, HEAD `a76a6f8`. Phase 05 non-goals apply throughout: no key rotation, no OAuth client credentials, no usage metering, no rate limits, no commercial entitlement behavior, no DynamoDB (Phase 06), no CDK/Secrets-Manager wiring (Phase 07), no operational hardening/audit read surface (Phase 08). **Re-baselined 2026-09-13 (post-approval delivery):** tasks 1–2 are now implemented in the working tree (uncommitted) and verified — suite green at **1087 tests** (943 baseline + 144 task-1/2 tests: `test_credentials` 84, `test_pepper` 19, `test_api_key_service` 30, `test_idgen` 66 incl. the extended `new_api_key_id` matrix), `ruff check`/`format` clean, HEAD still `a76a6f8`. Remaining implementation scope: tasks 3–8.

## Precondition and commit structure (re-baselined 2026-09-13)

HEAD is still `a76a6f8` and **nothing of Phase 05 is committed**. The working tree is dirty with (a) the Phase 04 close-out spec moves, (b) the **completed, verified** task 1 and task 2 implementations, and (c) this breakdown. The one-commit-per-task discipline therefore prescribes three commits before any new implementation work; the build-step must **not rewrite** the task 1–2 code unless a Verify bullet is found failing:

- **Commit 0 — `chore(specs)`** (the original precondition, unchanged in content): stage exactly the deletions `specs/wip/04-organizations-memberships-and-authorization.md`, `specs/wip/04-organizations-memberships-and-authorization-breakdown.md`, `specs/draft/04-organizations-memberships-and-authorization-breakdown.original.md`, `specs/draft/05-api-key-credentials-and-scopes.md`; additions `specs/done/04-organizations-memberships-and-authorization.md`, `specs/done/04-organizations-memberships-and-authorization-breakdown.md`, `specs/done/04-organizations-memberships-and-authorization-breakdown.original.md`, `specs/wip/05-api-key-credentials-and-scopes.md`, this breakdown, and the reviewer-preserved original `specs/draft/05-api-key-credentials-and-scopes-breakdown.original.md`. No `src/`, no `.kilo/`, no `.pi/`, no `NOTES.md` in this commit. Stage explicitly by path and verify `git status` shows exactly these paths before committing.
- **Commit 1 — `feat(phase-05 task 1)`**: `src/app/auth/credentials.py`, `src/app/auth/pepper.py`, `src/app/services/idgen.py` (+`new_api_key_id`), `src/app/auth/__init__.py` (re-exports), `src/tests/unit/test_credentials.py`, `src/tests/unit/test_pepper.py`, `src/tests/unit/test_idgen.py`. Pre-commit action: re-run task 1's Verify bullets (all present on disk and green — this is a verify-then-commit, not a write-then-commit).
- **Commit 2 — `feat(phase-05 task 2)`**: `src/app/services/api_key_service.py`, `src/tests/unit/test_api_key_service.py`. Pre-commit action: re-run task 2's Verify bullets (all present and green).

**Scaffold disposition (decision 0, superseded by delivery).** The 2026-09-13 review correctly found **no** Phase 05 sources on disk; since then tasks 1–2 were implemented **in full conformance with decisions 2/3/9/10** — verified function-by-function against this file: ULID `generate_key_id` (timestamp chars 1–10, first char ∈ `0–7`, randomness 11–26, no `_`), `secrets.token_urlsafe(32)` secret, first-`_`-split `parse_literal` + redacting `ParsedCredential`, lowercase-hex `hash_secret` + `dummy_secret_matches` (never authenticates, same crypto work), `StaticPepper` ≥32-byte enforcement + redaction, 44-char `key_prefix` per decision 2, sorted-unique scope normalization, `api_key.created`/`api_key.revoked` exact metadata shapes, audit-append strictly after the write, foreign-org `ApiKeyNotFoundError` raised **before** any CAS call, idempotent duplicate revoke with one truthful audit each. `src/app/auth/api_key_auth.py`, `src/app/auth/principal.py`, and `src/app/api/keys.py` remain absent — created fresh by tasks 3/5/6.

## Planner decisions (interpretation of the contract)

1. **The frozen surface is untouched — zero storage-contract changes.** Phase 02 already shipped the complete API-key persistence surface: `create_api_key`, `get_api_key` (by `key_` identity, deliberately org-unfiltered — the service enforces tenancy), `get_api_key_by_key_id` (§8 credential-segment point lookup), `list_api_keys` (org-scoped, all statuses, `(created_at, id)` order), and `revoke_api_key` (first-write-wins CAS, idempotent success, original `revoked_at` preserved, absence is `EntityNotFoundError`). `append_audit_event`'s docstring **names `api_key.revoked` as its intended Phase 05 standalone user**. The phase spec's generic task "update storage contract" is therefore discharged as *no new methods needed* — the 19-method protocol is consumed as-published, and Phase 06 inherits **no** new compound-replication obligation from Phase 05. AGENTS.md's "define and test atomicity and duplicate-request behavior" is satisfied by definition, not by a 20th method: creation is one row + one post-commit audit (single-row write is atomic; the key-created-audit-missing window is the Phase 04 decision-7 accepted limitation, documented); creation is **non-idempotent by design** (every POST mints a fresh credential — no client token invented); revocation's CAS is the idempotent duplicate path (decision 10). The compound option for creation was **considered and rejected**: §12 reserves compounds for multi-row batches whose partial state is unrecoverable (provisioning); key creation is one row (single-write atomic on every adapter) plus a post-commit audit — exactly the Phase 04 membership-audit pattern, and the published `append_audit_event` docstring already anticipates this phase's standalone appends. Likewise the phase spec's generic task "extend data models" is discharged as **no model extension needed**: the frozen §4 `ApiKey` (Phase 01) carries every Phase 05 field (`key_id`, `key_prefix`, `secret_hash`, derived-expiry `status`, lifecycle timestamps); the only model-layer addition this phase is the purely additive `actor_type_for` helper (decision 7). The spec task naming `services/audit_service.py` is honored by placing the key-audit builders in `services/api_key_service.py` beside their rules — the Phase 04 precedent is that each rules module owns its builders (`services/authorization.py` does); no new module is invented.

2. **Credential format (Phase 05 owns what Phase 01 deferred; spec §8 "recommended" made concrete).** Literal `fn_<live|test>_<key-id>_<secret>`:
   - `<key-id>`: 26-char **ULID** (48-bit ms timestamp + 80-bit `secrets`-drawn randomness, Crockford base32, uppercase, alphabet `0123456789ABCDEFGHJKMNPQRSTVWXYZ` — contains no `_`, which is what makes parsing unambiguous). Encoding pinned: 130 bits over 26 chars — timestamp in chars 1–10 (first char therefore ∈ `01234567`, a valid ULID needs no alphabet widening), randomness in chars 11–26; the task-1 shape test asserts exactly this. Satisfies the `KeyId` ≤64 bound; uniqueness is proven by the storage UNIQUE index (collision → 409, decision 11), not by entropy alone.
   - `<secret>`: `secrets.token_urlsafe(32)` — exactly **256 bits** of CSPRNG entropy, 43 base64url chars. The alphabet includes `_`, so parsing is: strip the fixed 8-char env prefix, split at the **first** `_` (key-id segment, underscore-free by charset), everything after is the secret (may contain `_`).
   - `key_prefix` display rule (Phase 01 deferred it): `fn_<env>_<key-id>_<first 6 secret chars>...` (length 8+26+1+6+3 = 44 ≤64 ✓; key-id is non-secret by §8 design). The 6-char (≈36-bit) secret head is display-masked, not disclosed-secret: the §8 example itself shows a truncated secret (`fn_live_01JXYZ7K_a8f...`), matching key-prefixes is Stripe/GitHub-standard identification, and the partial prefix is worthless offline because verification is `HMAC-SHA256(pepper, full-secret)` — without the pepper there is no check to grind, and the online path is rate-limitable (Phase 08). The full 43-char secret remains recoverable only from the one-time creation response.
   - `secret_hash`: `HMAC-SHA256(pepper, secret)` **lowercase hex** (Phase 01 left the encoding open; hex pinned here).
   - The `key_` application ID: `new_api_key_id()` added to `services/idgen.py` (same `prefix + "_" + uuid4().hex` strategy; the idgen docstring explicitly defers `key_` to Phase 05 — this discharges that half of the deferral; the §8 credential segment stays in `auth/credentials.py`, **not** idgen, so credential entropy never masquerades as application-ID entropy).
   - Full literal length: 8 + 26 + 1 + 43 = 78 ≤ the frozen `FullApiKey` 512 bound.

3. **Pepper through an injectable abstraction; no import-time config, no AWS import.** `src/app/auth/pepper.py`: `PepperSource` runtime-checkable `Protocol` with `current() -> bytes`; `StaticPepper` implementation validating **≥ 32 bytes** at construction and redacting itself everywhere (`__repr__`/`__str__` → `StaticPepper(<redacted>)`, never in exception messages, never logged/persisted/audited). Construction is pure (boot contract). Tests inject `StaticPepper`; **Phase 07** wires the AWS Secrets Manager implementation at the deployment entrypoint behind the same protocol — the no-`boto3`-in-`sys.modules` proof stays green because everything shipped in Phase 05 is stdlib (`hmac`, `hashlib`, `secrets`, `time`, `uuid`). Pepper rotation is a non-goal (one current version; `current()` is the seam that makes future rotation additive).

4. **Verification pipeline (spec §8 flow) and one uniform 401 — no failure oracle.** Fixed order: (a) parse literal (bad env prefix, missing/empty segments, key-id outside the Crockford 26-char shape, empty secret → format failure); (b) `get_api_key_by_key_id` — on `EntityNotFoundError` the pipeline still performs a **dummy constant-time comparison** of the supplied secret's HMAC against a fixed same-shape digest before failing (equalizes the unknown-key timing branch; the AC's "without leaking which secret segment failed" is honored on both the message *and* timing axes); (c) `hmac.compare_digest` of recomputed vs stored hash; (d) parsed env must equal stored `environment` (defense in depth against inconsistent rows); (e) `status is ACTIVE`; (f) `expires_at` is `None` or in the future (expiry **derived**, per the pinned enum decision — never a stored status). Every failure raises one `ApiKeyAuthenticationError` with the single fixed message `"invalid API key credentials"` → **401** `unauthenticated`; no branch-specific message, no key-id/secret/pepper material in any error, log, or audit. **Organization status and scopes are authorization, not authentication** — checked in the access dependency (decision 6), where denials get the Phase 04 uniform 403 + audit. This is the deliberate reading of AC 2's "disabled … rejected": the pinned `ApiKeyStatus` has no *disabled* value (only `active`/`revoked`), so "disabled" can only mean the organization — and organization status is exactly what Phase 04 renders as the uniform audited 403 (`inactive_organization`), mirroring Phase 03's disabled-**user** precedent (also 403, never 401). AC 2's oracle-freedom requirement ("without leaking which secret segment failed") therefore binds *within* each uniform class — one byte-identical 401 for every authentication failure, one byte-identical 403 for every access denial — not *between* them (a uniform-401-for-org-disabled rule would contradict the frozen Phase 03/04 behavior for the same condition on humans). `last_used_at` is **never written**: verification is read-only by design (the handoff's "no synchronous feednow-auth calls on every request" plus zero write amplification); the field stays `None` on every API-served row — escalated to §16/Phase 08, not silently dropped.

5. **API-key `AuthorizationContext` (spec §10; AC 4).** `build_api_key_context(api_key)`: `actor_type="api_key"`, `actor_id=api_key.id` (the `key_` application identity — never `usr_`, never the record's `key_id` segment), `organization_id=api_key.organization_id`, `roles=[]` **always** (the creator's memberships are never consulted — this is the structural "no human role escalation" proof), `scopes=` the key's stored (normalized) scopes. Human contexts are unchanged (Phase 03 `build_user_context`, `scopes=[]` stays empty for humans).

6. **Principal dispatch + the documented `organization_access` extension point.** New frozen `Principal` dataclass (`src/app/auth/principal.py`): `user: User | None`, `api_key: ApiKey | None`, `context: AuthorizationContext` (exactly one of user/api_key is set, enforced in `__post_init__`; on the human path it wraps the unchanged Phase 03 `ResolvedIdentity`). `build_current_principal(storage, verifier, pepper_source)` (`src/app/auth/dependencies.py`): extracts the bearer token via the existing `_extract_bearer_token`, then **dispatches on the literal prefix** `fn_live_`/`fn_test_` → API-key path (verify + context, failure → uniform 401); anything else → the unchanged Phase 03 chain. Prefix collision is impossible (JWS compact tokens always start `eyJ` — base64url of `{"`), documented. `organization_access` factories gain a **keyword-only `pepper_source: PepperSource | None = None`**: when `None` they compose exactly the Phase 03 chain (byte-stable regression: all Phase 04 tests pass **unchanged**, and a `fn_` literal on those routes simply fails JWT verification → 401); when provided, they compose `build_current_principal` and a non-`user` actor is refused with the uniform 403 **audited** `authorization.denied` reason `human_only` (generalized actor, decision 7). Ordering pinned: the `human_only` refusal moves **after** the organization fetch, so the audit has its FK target; an unknown organization still answers the same 403 with provably no audit row (the Phase 04 §16 structural exception, unchanged). The API-key branch of the scope dependency enforces the same precedence: org status → `organization_mismatch` → `insufficient_scope`, every denial audited when the org row exists. New factory `build_organization_scope_dependency(storage, verifier, pepper_source, required_scope, operation_id) -> PrincipalAccess` (new frozen dataclass: `principal`, `organization`) — **the Phase 06/07/08 handoff**: human branch = Phase 04 member-rank rules (roles govern, `required_scope` never applies to humans); API-key branch = org status → `api_key.organization_id == path org` → exact-string membership of `required_scope` in the context's scopes; every denial is the same uniform 403 body with its deterministic audit reason. `OrganizationAccess.membership` stays **non-optional** (Phase 04 type frozen — no shipped consumer reads it for keys because keys never receive `OrganizationAccess`; `PrincipalAccess` is the key-carrying type).

7. **Denial-audit generalization + reason vocabulary (explicit current-phase contract extension, not smuggled).** `build_denial_audit`/`audit_denial` (`services/authorization.py`) widen `actor_user_id: UserId` → `actor_id: ActorId` and derive `actor_type` from the concrete class via a new tiny model-layer helper `actor_type_for(actor_id: ActorId) -> ActorType` (`models/authorization_context.py`, additive; complements the existing `ensure_actor_id_matches_actor_type` single source). `AccessOutcome` gains exactly three denial strings — `human_only`, `organization_mismatch`, `insufficient_scope` — added to `DENIAL_REASONS`; the metadata shape stays `{"reason", "operation"}` (the Phase 04 §16 escalation pin). Call-site impact, verified against the tree: `audit_denial` is called **positionally** everywhere (`organization_access.py:123` + three unit tests) — the parameter rename is safe there. `build_denial_audit` is keyword-only and its **three** Phase 04 unit-test call sites (`test_authorization_rules.py` ~217/238/257, `actor_user_id=_ACTOR`) must be mechanically updated to `actor_id=` in the same commit — assertions unchanged, because a `UserId` actor still derives `actor_type="user"`. The shape test parametrizes over `DENIAL_REASONS` dynamically, so it auto-expands 4→7 cases with identical assertions. With those three keyword fixes, **all other Phase 04 tests pass unchanged** (positional `(storage, actor, org, reason, operation)` — rename only). API-key scope enforcement is **exact string match**, no wildcards, no hierarchy (spec §9 defines none; inventing `vispector:*` would be smuggled entitlement semantics — escalated instead).

8. **Management role policy (extends Phase 04 decision 3 to the new routes).** `GET .../api-keys` → member dependency (rank ≥ viewer); `POST .../api-keys` and `DELETE .../api-keys/{key_id}` → admin dependency (rank ≥ admin). Routers pass `pepper_source` so the key-rejection branch is live: an API key calling any management route gets the uniform 403 + `human_only` audit (AC 4's escalation proof). Path `{key_id}` carries the **`key_` application identity** (`ApiKeyId`) per the frozen manifest and `ApiKeySummary.id` — never the §8 credential segment (the manifest docstring pins this collision; the segment never appears in any path or response).

9. **Creation flow (service `create_api_key`, injectable `storage`/`now`/`pepper`/`ids`).** Normalize request scopes to **sorted unique** before persisting (the Phase 02 contract defers normalization to Phase 05; storage then round-trips exactly) → mint `key_` id, ULID key-id, 256-bit secret → build the complete `ApiKey` (`status=ACTIVE`, `created_by_user_id=` caller `usr_`, `key_prefix` per decision 2, `secret_hash` per decision 2/3, `expires_at=None` — the frozen §15 request schema has no expiry field, escalated) → `storage.create_api_key` → on success append `api_key.created` (metadata exactly `{"environment": <live|test>, "scopes": [<sorted scopes>]}` — non-secret; target `api_key`/the `key_` id; actor the human `usr_`) → return `(api_key, full_literal)`; the **router** is the only place the literal crosses into `ApiKeyCreatedResponse.key` (201), exactly once. Audit-append failure → 500 with the key persisted and the plaintext lost (documented decision-1 window). List: pass-through `list_api_keys`, project field-by-field to `ApiKeySummary` preserving `limit`/`next_cursor` verbatim (Phase 04 decision-8 pattern); summaries carry **no** `secret_hash`, no plaintext, no raw key-id segment.

10. **Revocation flow.** Admin dependency → `get_api_key(path id)` (`EntityNotFoundError` → domain `ApiKeyNotFoundError` → 404) → **`api_key.organization_id != path org` → the same 404 with the same fixed message** (no cross-org existence oracle; AC's isolation proof) → `storage.revoke_api_key(id, revoked_at=now)` (CAS: already-revoked is an **idempotent success**, original `revoked_at` preserved) → append `api_key.revoked` (metadata exactly `{}`; target the `key_` id) → **204** empty body (manifest-pinned). Duplicate/concurrent revocations each append one truthful audit row (each processed 204 *is* one audited action; suppressing the loser's audit would need winner-detection that same-clock ties defeat — defined, tested, documented). "Immediately effective" is structural: verification reads stored truth per request (no cache in this service), so the next request after the CAS sees `status=revoked` → 401.

11. **Error mapping (frozen envelope codes only).** Scope/name/environment shape failures → **422** via the frozen `RequestValidationError` handler (schemas are the single validator; `SCOPE_PATTERN` is reused, never re-declared). Invalid cursor → **400** `validation_error` (Phase 04 precedent). `DuplicateEntityError(kind=api_key_id | entity_id)` from create (ULID/uuid4 collision — astronomically rare but the UNIQUE index is the arbiter) → **409** `conflict` with a fixed retry message. Unknown/foreign key on revoke → **404** `not_found`. `ReferenceNotFoundError` from create (unknown org/creator FK) is **structurally unreachable** — the access dependency has just read the active organization row and the human actor's user row exists by construction (disable never deletes) — so it is deliberately *not* mapped: if it ever fires it is a bug and surfaces as the frozen 500, never a misleading 4xx. All auth failures → **401** `unauthenticated` (decision 4); all access denials → the one uniform **403** `forbidden` body (Phase 04 decision 4 — byte-identical across all eight denial reasons now). Any other `StorageError` → untranslated → frozen 500. Fixed messages only; no caller text, no credential material echoed.

12. **Test strategy (mirrors the proven Phase 03/04 stack).** Unit: pure credential functions (format round-trips, ULID shape/uniqueness, parse with `_`-bearing secrets, HMAC determinism, dummy-compare path taken, pepper masking/length), stub-`Storage` service tests (zero-write proofs, exact audit shapes, injected clock/ids), context builder (`roles == []`). Integration: `TestClient(create_app(routers=[...]))` with the signed-token + loopback `JwksTestServer` fixtures from `src/tests/support/cognito.py` (never live Cognito), real SQLite, Phase 04 seeded users/orgs/memberships (`create_api_key` targets seeded `usr_`s), `StaticPepper` injected; **probe routers defined in the test module** exercise `build_organization_scope_dependency` (shipped routers register manifest entries only). Expired keys are **seeded directly** (no API path exists — decision 9). Audit and row assertions read SQLite directly (no audit read surface). Concurrency: `threading.Barrier`, no sleeps, 20 repeats, main-thread warm-up. Every denial/401 case asserts byte-identical bodies (oracle-freedom); 403 cases assert the `authorization.denied` row where the org row exists (and provably none for unknown-org), while **401 authentication failures assert zero audit rows** (mirrors Phase 03: auth failures are not `authorization.denied` and §16 requires no auth-failure event); every secret-bearing path is followed by a substring sweep (no literal/secret/pepper bytes in any stored row, log record, or error body).

## Tasks

**Tasks 1–7 are delivered on disk (tasks 1–2 committed as 847bbe0 and its parent; tasks 3–7 uncommitted in the working tree, each approved at its step review). Task 8 (handoff docs + green sweep) is delivered — see "Delivery evidence (task 8)" below.** The pinned dependency order 3 → 4 → 5 → 6 → 7 → 8 (credentials→service→verify→generalize→dispatch→router→proofs→docs seam) was followed exactly.

1. **Credential primitives + pepper abstraction + `key_` minting**
   - **Status (2026-09-13): ✅ implemented, uncommitted — lands as commit 1 after a Verify re-run; no new code expected.**
   - Files: `src/app/auth/credentials.py` (new), `src/app/auth/pepper.py` (new), `src/app/services/idgen.py` (+`new_api_key_id`), `src/app/auth/__init__.py` (re-exports), `src/tests/unit/test_credentials.py`, `src/tests/unit/test_pepper.py`.
   - Content: decision 2 generators/parser (`generate_key_id` ULID, `generate_secret`, `build_literal`, `parse_literal` → frozen `ParsedCredential(environment, key_id, secret)` with `ApiKeyCredentialFormatError` on any shape violation), `hash_secret(pepper, secret) -> hex`, `secret_matches(stored_hash, pepper, candidate) -> bool` (constant-time; dummy-compare helper for the unknown-key branch), decision 3 `PepperSource`/`StaticPepper`.
   - Verify (unit): 10k generated key-ids unique + Crockford-26 shape; secret decodes to exactly 32 bytes; literal round-trip including a crafted secret containing `_`; wrong env token, missing prefix, empty segments, oversized inputs all raise the format error with no input echo; HMAC known-vector determinism (fixed pepper fixture constant in the test); `StaticPepper` rejects <32 bytes, repr/str contain no pepper bytes; `new_api_key_id` validates as `ApiKeyId`. `uv run pytest` green (943 + new), ruff clean.

2. **API-key service rules + audit builders**
   - **Status (2026-09-13): ✅ implemented, uncommitted — lands as commit 2 after a Verify re-run; no new code expected.**
   - Files: `src/app/services/api_key_service.py` (new), `src/tests/unit/test_api_key_service.py`.
   - Content: `create_api_key(storage, actor_user_id, organization_id, name, environment, scopes, *, pepper, now=None, ids=None) -> tuple[ApiKey, str]` (normalize → mint → build → persist → `api_key.created` audit after success, decision 9), `list_api_keys` pass-through, `revoke_api_key(storage, actor_user_id, organization_id, api_key_id, *, now=None)` (decision 10), domain errors `ApiKeyNotFoundError`/`ApiKeyConflictError`, builders `build_api_key_created_audit`/`build_api_key_revoked_audit` (exact metadata per decisions 9/10, ids/clock injected). No FastAPI imports.
   - Verify (unit, stub storage): zero writes when storage raises; persisted row's `model_dump()` contains no plaintext secret/literal substring and `secret_hash == hash_secret(...)`; scopes sorted-unique in row and audit; audit append happens **only after** a successful create/revoke and its failure propagates; duplicate-revoke path appends exactly one `api_key.revoked` per call; foreign-org revoke raises `ApiKeyNotFoundError` **before** calling `revoke_api_key` (recording stub proves no CAS call); deterministic under injected `now`/`ids`.

3. **Verification seam + API-key context resolution**
   - Files: `src/app/auth/api_key_auth.py` (new), `src/app/auth/__init__.py`, `src/tests/unit/test_api_key_auth.py`.
   - Content: `ApiKeyAuthenticationError` (one fixed message, decision 4), `verify_api_key(storage, pepper_source, literal, *, now=None) -> VerifiedApiKey(api_key, context)` running the pinned order with the dummy-compare on unknown key-id, `build_api_key_context` (decision 5), `key_has_scope(context, scope) -> bool` (exact match).
   - Verify (unit + SQLite-backed): matrix over valid, malformed-each-way, unknown key-id, wrong secret (1-bit flip), env-prefix/store mismatch, revoked, expired (seeded `expires_at` past), boundary `expires_at == now` (expired — `<` vs `<=` pinned) → every failure raises the **identical** message; success context has `actor_type="api_key"`, `roles == []`, stored scopes; dummy-compare call-count proof; no key-id/secret substring in any exception text.

4. **Denial-audit generalization + extended reason vocabulary** (decision 7; lands before any API-key consumer exists, mirroring the Phase 04 contract-before-consumer discipline)
   - Files: `src/app/models/authorization_context.py` (+`actor_type_for`), `src/app/services/authorization.py`, `src/tests/unit/test_authorization_rules.py` (extend + the three pinned keyword updates).
   - Content: `build_denial_audit`/`audit_denial` `actor_user_id: UserId` → `actor_id: ActorId` with `actor_type` derived via `actor_type_for`; `AccessOutcome` + `DENIAL_REASONS` gain `human_only`, `organization_mismatch`, `insufficient_scope`; metadata shape stays `{"reason", "operation"}`. No behavior change for human actors.
   - Verify: `actor_type_for` round-trips `UserId`→`user`, `ApiKeyId`→`api_key` and rejects anything else; `build_denial_audit` with a `key_` actor yields `actor_type="api_key"`, actor `key_`, org FK, pinned metadata; the three keyword call sites updated mechanically (assertions untouched); the dynamic `DENIAL_REASONS` parametrize now covers 7 reasons with identical shape assertions; `granted` still rejected as a reason; every other Phase 04 test passes **unchanged**; `organization_access.py:123` positional call unaffected.

5. **Principal dispatch + `organization_access` API-key branch + scope dependency** (decisions 6/8)
   - Files: `src/app/auth/principal.py` (new), `src/app/auth/dependencies.py` (+`build_current_principal`), `src/app/auth/organization_access.py`, `src/app/auth/__init__.py`, `src/tests/unit/test_principal_dispatch.py` (new), `src/tests/unit/test_organization_access.py` (extend).
   - Content: `Principal`, prefix dispatch, `pepper_source=None`-compatible factories, `human_only` refusal (post-org-fetch ordering per decision 6), `build_organization_scope_dependency`/`PrincipalAccess` with the api-key precedence (org status → `organization_mismatch` → `insufficient_scope`) and human member-rank branch.
   - Verify: **all Phase 04 access tests pass unchanged** (byte-stability proof of the `pepper_source=None` default — the only Phase 04 test-file change this task is additive); probe-router tests — key+scope 200; key w/o scope 403+audit `insufficient_scope` (actor `key_`); key of org A on org B 403+audit `organization_mismatch`; key on member/admin factories (pepper wired) 403+audit `human_only`; key on member/admin factory with pepper **not** wired → 401 (JWT path, Phase 04 behavior); disabled org with key 403+audit `inactive_organization` under a `key_` actor (proves the generalization); unknown org + key → 403 provably **no** audit row (the §16 exception holds for key actors too); human on scope factory ignores `required_scope` and follows rank rules; all denial bodies byte-identical to the Phase 04 pinned 403; JWT-looking vs key-looking bearer dispatch boundary (`eyJ…` never parsed as key, `fn_…` never sent to the JWT verifier).

6. **Keys router (create/list/revoke)**
   - Files: `src/app/api/keys.py` (new), `src/app/api/__init__.py` (if re-export pattern needs it), `src/tests/integration/test_api_keys_endpoints.py`.
   - Content: `build_api_keys_router(storage, verifier, pepper_source)` registering `list_api_keys`/`create_api_key`/`revoke_api_key` from `endpoint_for` specs (manifest-pinned paths, statuses 200/201/204, path-param types); member dependency on list, admin dependency on create/revoke (both with `pepper_source` wired, decision 8); service calls with injected storage; decision-11 mapping; `ApiKeySummary`/`ApiKeyCreatedResponse` projections.
   - Verify (integration): create 201 returns `{id, name, key, created_at}` and the literal verifies via `verify_api_key`; direct SQLite read shows only the HMAC (no plaintext substring in the file); the literal appears in **no** other response (list/GET bodies swept); list shows all statuses org-scoped with masked prefix, cursor round-trip, foreign cursor 400; role matrix (owner/admin 201/204; member/viewer 403+`insufficient_role` audit; outsider 403+audit; API-key bearer on all three routes 403+`human_only` audit); revoke → 204, second revoke → 204 + second truthful audit, subsequent `verify_api_key` → uniform 401, list shows `status=revoked` + `revoked_at`; foreign/unknown key on revoke → byte-identical 404s; invalid scope shape → 422; every error matches the frozen envelope.

7. **Acceptance proofs: secrecy sweeps, verification matrix over HTTP, concurrency**
   - Files: `src/tests/integration/test_api_key_secrecy.py`, `src/tests/integration/test_api_key_auth_matrix.py`, `src/tests/integration/test_api_key_concurrency.py`, extend `src/tests/integration/test_audit_hygiene.py`.
   - Content: (a) run the full battery with `caplog` at every level + direct reads of **every** `api_keys`/`audit_events` row and every error body: no literal, secret, key-id segment, or pepper byte anywhere (hygiene extension: actions ⊆ §16 vocabulary incl. `api_key.created`/`api_key.revoked`, metadata keys ⊆ pinned sets, incl. the three new denial reasons); (b) decision-4 matrix through a probe route: invalid format / unknown / expired / revoked / env-mismatch / wrong secret → byte-identical 401, zero storage mutation (recording counts); (c) barrier cases (20 repeats): 8 concurrent revokes → one stored `revoked_at` (winner), all 204, 8 audits, interleaved verify goes 200→401 and never back; 8 concurrent creates → 8 distinct keys/literals, 8 audits; forced key-id collision (monkeypatched generator) → 409 + zero audit rows.
   - Verify: no sleeps; stable across repeats; counts via direct SQLite reads.

8. **Handoff documentation and green sweep**
   - Files: `docs/phases/05-api-keys.md` (new), `docs/README.md`, `docs/architecture.md`, `docs/contracts.md`, `README.md`, this breakdown (evidence + DoD check).
   - Content: the contracts Phases 06/07/08 code against — credential format + parse contract (decision 2), `PepperSource` (**Phase 07 obligation**: Secrets Manager implementation at the entrypoint, ≥32-byte enforcement, never in env dumps), `verify_api_key` + `build_current_principal` + `build_organization_scope_dependency` (the reusable authorizer seam — in-process, read-only, no per-request HTTP call), the §10 context shapes, extended denial vocabulary + audit metadata, revoke CAS/duplicate semantics. Known limitations: `last_used_at` never set (no storage write path — decision 4), expiry not settable via API (frozen §15 schema), no rotation/wildcards/self-management, audit-append-after-commit window, duplicate truthful revoke audits, key survives creator-user disable (no coupling invented — documented, Phase 08 may revisit). Follow `docs/requirements.md`: no links from `docs/` to WIP/draft specs.
   - Verify: `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `git diff --check` green; no-`boto3` subprocess proof stays green (Phase 05 adds **no runtime dependencies** — stdlib only); record command-output summary as evidence.

## Definition of done check (maps to acceptance criteria)

- [x] Full plaintext returned only by successful creation, exactly once; list/reads masked (tasks 2, 6, 7 — one-crossing-point router + body/file sweeps)
- [x] Invalid format, unknown, expired, revoked, disabled(org), mismatched secrets rejected without segment oracle (tasks 3, 5, 7 — uniform 401 + timing-equalized dummy compare + byte-identical bodies; org-disabled rides the uniform audited 403 class per decision 4's AC-2 reading)
- [x] Revocation immediately effective + `api_key.revoked` (tasks 2, 6, 7 — CAS + fresh-read proof + barrier)
- [x] Human contexts expose roles; keys expose org/actor/scopes with no role escalation (tasks 3, 5, 6 — `roles==[]` pin + `human_only` matrix)
- [x] Tests prove non-persistence/non-logging, env prefixes, scope enforcement, cross-org isolation (tasks 1, 3, 5, 7 — matrix + hygiene extension + `organization_mismatch`)

## Delivery evidence (task 8)

Commits: precondition `chore(specs)` (`86c21a1`, specs moves only), then
`feat(phase-05 task 1)` (`b9d4901`), `feat(phase-05 task 2)` (`847bbe0`),
and tasks 3–8 delivered in the working tree (each step reviewer-approved;
task 7's F1–F5 corrections applied — WAL-inclusive raw-file sweep,
sample-then-check-stop watcher, tautology removed, `caplog.text`, comment
fixed). The task 3–8 commits land per this breakdown's file lists.

Final sweep (2026-09-14):

- `uv run pytest` → **1253 passed** (baseline 943 + 310 Phase 05: tasks 1–2
  146 — `test_credentials` 84, `test_pepper` 19, `test_idgen` +11,
  `test_api_key_service` 32; task 3 34; task 4 +17 (`test_authorization_rules`
  69→86); task 5 +28 (`test_principal_dispatch` 15,
  `test_organization_access` 7→20); task 6 20; task 7 +65
  (`test_api_key_secrecy` 4, `test_api_key_auth_matrix` 18,
  `test_api_key_concurrency` 42, `test_audit_hygiene` 2→3)).
- `uv run pytest src/tests/storage_contract` → 64 passed **unchanged**
  (decision 1 honored: Phase 05 adds no storage methods; the five api-key
  ops — key_id UNIQUE, revoke CAS — were already adapter-neutral suite
  cases for Phase 06).
- `uv run ruff check .` → All checks passed; `uv run ruff format --check .`
  → 129 files clean; `git diff --check` → clean.
- No-`boto3` subprocess import proof (`test_app_skeleton.py`) green;
  Phase 05 added no runtime dependencies (`pyproject.toml`/`uv.lock`
  untouched — stdlib only, decision 3).
- Handoff: `docs/phases/05-api-keys.md` (new) linked from `docs/README.md`;
  `docs/architecture.md`, `docs/contracts.md`, and `README.md` updated in
  the same step; no docs link to WIP/draft specs. Known limitations
  recorded there: `last_used_at` never set (read-only verification, no
  storage write path), expiry enforced but not settable via the frozen §15
  request, no rotation/wildcards/self-management, audit-append-after-commit
  window, duplicate truthful revoke audits, key survives creator-user
  disable (no coupling invented — Phase 08 candidate).

Escalations below remain open as spec-revision proposals (planner record,
not implementation work); Phase 06/07 obligations and handoff notes are
recorded in the phase doc's "Published interfaces" and this file.

Correction note (2026-09-14, task 8): the re-baseline review-line above
records **1087** with `test_api_key_service` 30 (+144); the committed
task-2 file collects **32** (+146) — the two additional tests landed
between that measurement and commit `847bbe0`. The recorded results are
not rewritten; the current-state counts in this section are authoritative.

## Escalations (planner action, not implementation work)

- **Spec-revision proposals** (delivery-map review rule): (1) §8 — pin the concrete credential format decision set (ULID key-id, base64url 256-bit secret, hex HMAC encoding, `key_prefix` truncation rule) that the spec leaves "recommended"; (2) §15 — the create request has no `expires_at` field although §4/§8 verification semantics require expiry enforcement (Phase 05 enforces on seeded rows only); (3) §16 — pin `api_key.created` metadata `{environment, scopes}`, `api_key.revoked` metadata `{}`, the three new denial reasons, and that duplicate revocations append one truthful audit each; (4) §14 — name the revoke outcomes the spec leaves open (unknown/foreign → 404; already-revoked → idempotent 204); (5) §4 — `last_used_at` has no write path in any phase plan (needs either a §12 compound or an async usage-recording design — Phase 08 candidate).
- **Phase 06 obligation:** none new — Phase 05 adds no storage methods; the existing five api-key ops (incl. `api_keys_key_id_unique` + revoke CAS) are already in the conformance suite the DynamoDB adapter must pass.
- **Phase 07 handoff note:** deployment entrypoint wires `StaticPepper`→Secrets Manager `PepperSource` and mounts `build_api_keys_router(storage, verifier, pepper_source)`; product authorizers import `build_organization_scope_dependency`.
- **Deferred, explicit (not smuggled):** rotation, OAuth client credentials, metering/rate limits (phase non-goals); scope wildcards/hierarchy; API-key self-management; revoking keys when their creator is disabled; audit read surface (Phase 08).

## Review log

- **@reviewer 2026-09-13: APPROVE-WITH-CHANGES (applied).** All on-disk claims re-verified true: `uv run pytest` → **943 passed**, ruff check/format clean, HEAD `a76a6f8`; `git status --porcelain` shows exactly the four Phase 04 close-out deletions, three `specs/done/04-*` additions, both `specs/wip/05-*` files, and `.kilo` (excluded) — decision 0's "no abandoned stubs" holds (`keys.py`/`api_key_service.py`/`credentials.py`/`pepper.py`/`api_key_auth.py`/`principal.py` all absent); the frozen contract exposes exactly **19** methods with the five api-key ops and every docstring pin quoted in decision 1 verified verbatim (org-unfiltered `get_api_key`, §8 point lookup, `(created_at, id)` order, first-write-wins CAS with original `revoked_at`, absence → `EntityNotFoundError`, `append_audit_event` naming `api_key.revoked` as its Phase 05 standalone user); `test_storage_contract.py:456` pins `len(members) == 19`; `api_keys_key_id_unique` exists and is already exercised in the conformance suite (Phase 06 inherits nothing new); manifest `_KEY_PATH_PARAMS` maps `{key_id}` → `ApiKeyId` with 200/201/204 and the collision docstring at `manifest.py:11-14`; `organization_access.py:31-35` carries the Phase 05 extension point and its factories' current `(storage, verifier, operation_id)` shape makes the keyword-only `pepper_source=None` addition source-compatible; `KeyId`/`KeyPrefix` ≤64, `SecretHash`/`FullApiKey` ≤512 confirmed (78-char literal and 44-char prefix fit); `idgen.py` defers `key_` to Phase 05 verbatim; §8/§14/§15/§16 spec text matches every quoted claim. Changes applied to this file (original preserved at `specs/draft/05-api-key-credentials-and-scopes-breakdown.original.md`, to be swept into the precondition commit): **B1** — decision 7's "parameter rename only, tests keep passing" was false on disk: `build_denial_audit` is keyword-only and three `test_authorization_rules.py` call sites pass `actor_user_id=`; the decision now pins the mechanical same-commit rename, the positional-safety of `audit_denial`, and the 4→7 auto-expansion of the dynamic `DENIAL_REASONS` parametrize. **B2** — old task 4 (9 files, two independent seams) split into task 4 (audit generalization, contract-before-consumer per the Phase 04 pattern) and task 5 (principal dispatch + scope dependency); tasks renumbered 5→6, 6→7, 7→8 and all DoD references updated. **B3** — decision 6 now pins the `human_only` refusal **after** the org fetch (the audit needs its FK target; unknown-org+key keeps the documented unauditable exception) and task 5 verifies both the key-with-pepper-wired 403+audit and the pepper-absent 401 boundary. **B4** — decision 1 extended to also discharge the phase spec's "extend data models" and "audit_service.py" tasks explicitly (frozen §4 model suffices; builders follow the Phase 04 per-module precedent) and to record the compound-for-creation option as considered-and-rejected. **B5** — decision 2 now pins the ULID char layout (timestamp chars 1–10, first char ∈ `0–7`) and grounds the 6-char `key_prefix` disclosure in §8's own example plus the pepper-keyed-HMAC argument. **B6** — decision 4 states the AC-2 "disabled" reading explicitly (no `disabled` key status exists; org-disabled = audited uniform 403, mirroring Phase 03's disabled-user 403; oracle-freedom binds within each uniform class). **B7** — decision 11 pins create-time `ReferenceNotFoundError` as structurally unreachable → frozen 500. **B8** — decision 12 pins 401 cases to assert **zero** audit rows (auth failures are not `authorization.denied`). Verdicts on the eight scrutiny areas: (1) zero-storage-change — **approve** (AGENTS.md names *revocation* as the concurrency-sensitive op and the CAS defines/tests it; creation is single-row-atomic, duplicate behavior defined as non-idempotent-by-design, symmetric with Phase 04's accepted post-commit audit window; a 20th compound would tax Phase 06 for no recoverability gain); (2) credential format — **approve after B5** (parse is unambiguous: underscore-free Crockford 26-char segment split at the first `_` after strip; 43-char base64url secret may contain `_`; HMAC-with-pepper makes the 6-char display head inert offline); (3) uniform-401 boundary — **approve after B6** (401/403 split is consistent with AC 2's wording and both frozen precedents); (4) pepper_source seam + audit generalization — **approve after B1/B3** (seam is byte-stable; the generalization's real churn is now honestly costed); (5) duplicate truthful `api_key.revoked` audits — **approve** (each processed 204 *is* an audited action; winner-detection is impossible under same-clock ties; escalated for §16 pinning); (6) task ordering/granularity — **approve after B2** (8 commits, each independently green, dependency order credentials→service→verify→generalize→dispatch→router→proofs→docs); (7) precondition/scaffold claims — **approve** (verified against `git status`); (8) AC coverage — **approve** (all five ACs map to pinned tests after the DoD renumbering; no phase-spec task left undischarged after B4).

- **@planner 2026-09-13 (re-baseline, for @reviewer):** delivery moved after the APPROVE-WITH-CHANGES pass — tasks 1–2 are implemented in the working tree and verified green (**1087 passed**, ruff clean, HEAD `a76a6f8` unchanged); the precondition section was rewritten as the three-commit structure (commit 0 `chore(specs)` — content unchanged, commit 1 task 1, commit 2 task 2) under verify-then-commit discipline; decision 0's scaffold claim is superseded by the delivery-state note (implementation conformed to decisions 2/3/9/10, spot-verified); tasks 3–8 are **unchanged** from the approved text and remain the whole of the implementation scope. Requesting re-approval of the re-baselined plan (on-disk claims + commit structure + remaining-part coverage of ACs 1–5).
- **@reviewer 2026-09-14: APPROVE (re-baseline approved — build-step may proceed with commits 0/1/2, then task 3).** No changes applied to this file; the re-baseline is accepted as written. All claims independently re-verified against the tree: (1) **On-disk state** — `uv run pytest` → **1087 passed**, `ruff check .` and `ruff format --check .` (119 files) clean, `git rev-parse --short HEAD` = `a76a6f8`; per-file collects match the claim exactly (`test_credentials` 84, `test_pepper` 19, `test_api_key_service` 30, `test_idgen` 66 vs 55 baseline → +144 new). `git status --porcelain` is precisely the described tree: 4 Phase-04/draft deletions, 3 `specs/done/04-*` additions, both `specs/wip/05-*` files plus the `.original.md` draft copy, 9 dirty `src/` paths, and untracked `.kilo` — commit 0's explicit stage-list is complete and exact (no `src/`, no `.kilo/`, no `NOTES.md` — the latter doesn't exist in status), and commits 1–2 cover all nine `src/` paths with nothing orphaned (commit 1: `credentials.py`, `pepper.py`, `idgen.py`, `auth/__init__.py`, `test_credentials.py`, `test_pepper.py`, `test_idgen.py`; commit 2: `api_key_service.py`, `test_api_key_service.py`). (2) **Task 1–2 conformance spot-audit — the supersession claim is true.** `credentials.py`: Crockford alphabet contains no `_`; `generate_key_id` is the pinned 130-bit encoding (48-bit ms timestamp in chars 1–10, first char ∈ `0–7` by the two zero pad bits, `secrets.randbits(80)` in chars 11–26); `parse_literal` strips the fixed 8-char prefix and `partition`s at the **first** `_` with exact Crockford-26 shape validation on the key-id segment and `_`-tolerant secret; `hash_secret` is lowercase-hex HMAC-SHA256; `dummy_secret_matches` performs the identical hash+`compare_digest` work against `"0"*64` — a digest no achievable HMAC output equals, so it can never authenticate; `ParsedCredential` and the format error carry one fixed, input-echo-free message (repr/str redact the secret; `build_literal` avoids echo by membership-checking instead of raising through `ValueError.__context__`). `pepper.py`: `PepperSource` runtime-checkable protocol, `StaticPepper` enforces ≥32 bytes at pure construction, copies bytearray input, `__repr__`/`__str__` → `StaticPepper(<redacted>)`, short-pepper error names only the floor. `idgen.py`: `new_api_key_id` uses the established `_mint(ApiKeyId)` strategy and the deferral docstring is discharged in both halves; the credential segment stays out of idgen (test-pinned). `api_key_service.py`: create order is normalize → mint (injectable bundle, secret redacted in repr) → build (`status=ACTIVE`, `expires_at=None`, 44-char `key_prefix`, `secret_hash`) → `storage.create_api_key` → `api_key.created` **strictly after** → literal assembled from stored segments; `DuplicateEntityError` → `ApiKeyConflictError`, any other storage error propagates untranslated with zero audits; revoke is get → tenancy check raising the same `ApiKeyNotFoundError` **before any CAS call** → CAS (idempotent, original `revoked_at` preserved) → exactly one `api_key.revoked` per processed call; audit metadata asserted with exact equality (`{"environment", "scopes"}` / `{}`); no FastAPI imports; service touches only the frozen 19-method storage contract. The 144 tests are not weaker than the Verify bullets: sequencing is proven via recorded call order (`["create_api_key", "append_audit_event"]`), the no-CAS proof via `"revoke_api_key" not in storage.calls`, dummy-compare equal-work via a monkeypatched hash counter, 10k uniqueness/shape, 32-byte secret decode, `_`-bearing round-trips, known-vector HMAC, and no-echo sweeps over exception chains. (3) **Tasks 3–8 unchanged** — `diff` against `specs/draft/05-api-key-credentials-and-scopes-breakdown.original.md` shows the only deltas since approval are the three claimed re-baseline edits (header, precondition/commit-structure section, scaffold-disposition paragraph, task 1–2 status lines) plus the previously applied B1–B8 changes; task 3–8 bodies, all twelve planner decisions, the DoD check, and the Escalations are untouched (tasks 6–8 differ from the pre-B original only by the B2 renumbering). (4) **AC coverage survives** — the five phase-spec ACs map to tasks 2/6/7, 3/5/7, 2/6/7, 3/5/6, 1/3/5/7 respectively; delivered tasks 1–2 discharge the primitives/rules halves, and every remaining obligation lands in tasks 3–8 in the pinned order 3→4→5→6→7→8. (5) **AGENTS.md boundaries** — no plaintext secret, pepper, or literal can reach persistence or logs on any shipped path (HMAC-hex-only rows, redacted reprs, fixed messages); internal `key_`/`usr_`/`org_` IDs only (the §8 segment never leaves the credential layer); services depend only on the storage contract; Phase 06/07/08 obligations remain confined to the Escalations section and docstring notes (no `boto3`, no Secrets Manager import, no compound methods invented). Verify-then-commit discipline (no rewrite of task 1–2 code unless a Verify bullet fails) is the correct framing and is honored by this approval.
- **@reviewer 2026-09-14 (task 7 step review): APPROVE-WITH-CHANGES.** Independently re-verified on the tree: `uv run pytest` → **1253 passed**, the four task-7 files → **67 passed** re-run 3× (~4 s, barrier-based, no sleeps), `ruff check .` clean, `ruff format --check .` (128 files) clean, `git diff --check` clean. No production file changed in this step (mtimes: all `src/app/` edits ≤ 11:29, task-7 test edits 12:07–12:15; `build_key_prefix`/`new_api_key_creation_ids` confirmed already in committed task-2 code, and the service reads the clock **once** per revoke so CAS `revoked_at` and audit `created_at` are the same object — the winner-timestamp membership pin is sound; the router passes no `ids=`, so the case-3 monkeypatch of the module-global `new_api_key_creation_ids` is the real call path). **Conformance:** the key_prefix exemption is decision-2-legal and narrow — only `*.segment` entries are exempted, only where §8 designed them to live (masked `key_prefix` values in responses, with a compensating per-column pin that no other `api_keys` column carries any segment; raw persisted-row, audit, and hygiene sweeps keep the strict rule for segments, and literal/secret/pepper keep the strict rule everywhere including the owning 201 vs foreign-key cross-check); `caplog.format` genuinely does not exist on pytest 9.1.1 `LogCaptureFixture` (verified), so the `record.getMessage()` join is a legitimate fix; the `key_denials` actor-set correction is right (the `insufficient_scope` denial belongs to seeded `key_noscope`); revoked-metadata `[..] == [{}, {}]` is the correct dict comparison; the hygiene extension is purely additive (11 removed lines are docstring/`__init__` shape only — Phase 04 batteries and assertions unchanged) and its mixed-actor battery pins §16 vocabulary, exact metadata, both-actor identity agreement, and strict segment absence in audits. Concurrency case 1 pins all four decision-10 semantics (8×204, one stored `revoked_at` ∈ audit timestamps, 8 truthful audits, monotonic 200→401 never-back with deterministic post-race re-verify); case 2 pins non-idempotent creation (8 distinct ids/segments/secrets/literals, direct-read row/audit equality); case 3 pins decision 11 for **both** collision kinds with zero-audit/zero-mutation direct reads. **Required change before the task-7 commit — F1:** the raw-database sweep (secrecy test, section (5)) reads only `env.db_path.read_bytes()` while the storage connection is still open, but the adapter runs `PRAGMA journal_mode = WAL` (`sqlite.py:607`) — proven by repro: battery-written content (and any would-be plaintext leak, plus pre-revoke row versions) lives in `secrecy.sqlite-wal`, **not** in the main file, which is near-empty schema. The docstring's "(WAL included)" claim is therefore false and the sweep is vacuous for its purpose. Fix: scan every `secrecy.sqlite*` file (main + `-wal` + `-shm`) for the non-exempt materials, keeping the existing segment exemption (segments legitimately persist in `key_id`, WAL included). **Non-blocking hardening — F2:** make the watcher's post-commit sample deterministic by reordering its loop to sample-then-check-stop (`while True: …; if stop.is_set(): break`) so `False in trace` is structurally guaranteed rather than timing-overwhelming. **Nits — F3:** `test_api_key_secrecy.py:728` `>= set()` is a tautology (the next line does the work; delete it); **F4:** prefer `caplog.text` over the `getMessage()` join (formats exception text too — strictly stronger); **F5:** concurrency module comment says "32-byte" pepper for a 36-byte constant. Task-7 text and the rest of the plan unchanged; build-step applies F1 (F2–F5 at the same pass), then the phase proceeds to task 8.
